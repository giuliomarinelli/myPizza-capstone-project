import { ProductErrorMessage } from './product-error-message';

describe('ProductErrorMessage', () => {
  it('should create an instance', () => {
    expect(new ProductErrorMessage()).toBeTruthy();
  });
});
